package ${packageName};

import com.elionyz.common.base.BaseService;
import ${pojo_packageName}.${className};
/**
 * @author Xiaobei
 * @version ${version}
 * @project ${project}
 * @class_name ${className}
 * @date ${.now?string("yyyy-MM-dd HH:mm")}
 */
public interface ${className}Service extends BaseService<Integer,${className}>{
}