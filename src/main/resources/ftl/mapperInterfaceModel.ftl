package ${packageName};

import com.elionyz.common.base.BaseMapper;
import ${pojo_packageName}.${className};
/**
 * @author Xiaobei
 * @version ${version}
 * @project ${project}
 * @table_name ${tableName}
 * @date ${.now?string("yyyy-MM-dd HH:mm")}
 */
public interface ${className}Mapper extends BaseMapper<Integer,${className}> {

}